# Existing
pandas>=2.0.0
requests>=2.28.0
yfinance>=0.2.0
flask>=3.0.0
flask-cors>=4.0.0

# LLM Providers (install only what you use)
anthropic>=0.25.0
google-generativeai>=0.5.0
# Ollama is accessed via its local REST API — no pip package needed

# Sentiment & News
praw>=7.7.0          # Reddit API client
